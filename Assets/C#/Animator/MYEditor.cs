using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

//[CustomEditor(typeof(Player_State))]
public class MYEditor : Editor {
    /*public override void OnInspectorGUI()
    {
        var target = (Player_State)(serializedObject.targetObject);
        target.groundedDistance = EditorGUILayout.FloatField("asdasd", target.groundedDistance);
        
        
        Rect rect = new Rect(20, 150, 300, 50);
        EditorGUILayout.LabelField("fgdgf", GUILayout.Width(400), GUILayout.Height(30));
        target.groundedCheckOffset = EditorGUI.FloatField(rect,"偏移", target.groundedCheckOffset);target.attack = EditorGUILayout.IntSlider("攻击力", target.attack, 0, 100,GUILayout.ExpandWidth(true));
        //EditorGUI.DrawRect(rect, Color.grey);
    }*/

}
